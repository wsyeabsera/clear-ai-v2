id: simple-contaminants-002
name: "List contaminants by risk level"
category: simple
description: |
  Test contaminant listing filtered by risk level.
  Verifies risk_level filter (low, medium, high, critical).

tags:
  - contaminants_list
  - filter
  - risk

priority: critical

query: "Show me all high risk contaminants"

expectedBehavior:
  toolsUsed:
    - contaminants_list
  minResults: 0
  maxLatencyMs: 15000
  responseContains:
    - contaminant
    - high
    - risk
  analysisRequired: false

validation:
  - type: tool_selection
    expected:
      - contaminants_list
  - type: performance
    maxLatencyMs: 15000

