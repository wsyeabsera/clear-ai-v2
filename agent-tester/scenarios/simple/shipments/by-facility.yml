id: simple-shipments-004
name: "List shipments by facility"
category: simple
description: |
  Test shipment listing filtered by facility.
  Verifies facility_id filter handling.

tags:
  - shipments
  - filter
  - facility

priority: high

query: "Show me shipments for facility FAC-001"

expectedBehavior:
  toolsUsed:
    - shipments_list
  minResults: 0
  maxLatencyMs: 15000
  responseContains:
    - shipment
    - FAC-001
  analysisRequired: false

validation:
  - type: tool_selection
    expected:
      - shipments_list
    allowExtra: true
  - type: performance
    maxLatencyMs: 15000

