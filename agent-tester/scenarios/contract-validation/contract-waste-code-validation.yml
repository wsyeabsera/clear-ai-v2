id: contract-validation-005
name: "Validate waste codes against contract declarations"
category: contract-validation
description: |
  Test validating detected waste codes against contract declarations.
  Tests European waste code validation and compliance checking.

tags:
  - contracts
  - waste-codes
  - validation
  - european-codes

priority: high

query: "Validate that all detected waste codes in recent shipments match their contract declarations"

expectedBehavior:
  toolsUsed:
    - contracts_list
    - shipment_loads_list
    - shipment_compositions_list
  minResults: 0
  maxLatencyMs: 20000
  responseContains:
    - waste
    - code
    - validation
  analysisRequired: true

validation:
  - type: tool_selection
    expected:
      - contracts_list
      - shipment_loads_list
      - shipment_compositions_list
    allowExtra: true
  - type: performance
    maxLatencyMs: 20000
  - type: response_content
    mustContain:
      - "waste"
      - "code"
