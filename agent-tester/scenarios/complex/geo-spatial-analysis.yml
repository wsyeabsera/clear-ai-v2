id: complex-016
name: "Geographic distribution analysis"
category: complex
description: |
  Analyze geographic distribution of facilities and shipments.
  Tests handling of location-based queries.

tags:
  - complex
  - geographic
  - distribution
  - spatial

priority: medium

query: "Analyze the geographic distribution of contaminated shipments across Germany"

expectedBehavior:
  toolsUsed:
    - facilities_list
  minResults: 0
  maxLatencyMs: 20000
  responseContains:
    - shipment
  analysisRequired: true

validation:
  - type: tool_selection
    expected:
      - facilities_list
    allowExtra: true
  
  - type: performance
    maxLatencyMs: 20000

